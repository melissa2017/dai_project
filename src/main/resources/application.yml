#spring.h2.console:
#  enabled: true
#  path: /h2-console

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/announcement_app
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL92Dialect

server:
  port: 8083
